import { ReactNode } from 'react';

const ChatLayout = ({ children }: { children: ReactNode }) => {
    return (
        <>
            ChatLayout
            {children}
        </>
    );
};

export default ChatLayout;
